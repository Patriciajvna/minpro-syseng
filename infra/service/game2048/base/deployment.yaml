apiVersion: apps/v1
kind: Deployment
metadata:
  name: minpro
spec:
  selector:
    matchLabels:
      app: minpro
  template:
    metadata:
      labels:
        app: minpro
    spec:
      containers:
      - name: minpro
        image: patriciajovena/minpro-syseng:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: "1Mi"
            cpu: "1m"
          limits:
            memory: "500Mi"
            cpu: "500m"
        ports:
        - containerPort: 8000
        # livenessProbe:
        #   httpGet:
        #     path: "/v1/ping"
        #     port: 8080
        #   initialDelaySeconds: 30
        #   periodSeconds: 30
        # readinessProbe:
        #   httpGet:
        #     path: "/v1/ping"
        #     port: 8080
        #   initialDelaySeconds: 30
        #   periodSeconds: 30
      # imagePullSecrets:
      #   - name: melissa-hol-group
---
apiVersion: v1
kind: Service
metadata:
  name: minpro-svc
spec:
  selector:
    app: minpro
  ports:
  - port: 80
    targetPort: 8000
