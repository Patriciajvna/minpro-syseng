apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minpro-ingress
spec:
  rules:
  - host: minpro.shamirhusein.my.id
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: minpro-svc
            port: 
              number: 80
  ingressClassName: nginx
